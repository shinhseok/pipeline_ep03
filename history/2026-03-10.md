# History: 2026-03-10

## 1. [00:10] MCP 인코딩 버그 수정

### 작업 지시
- `/run-planning` 실행 시 NotebookLM MCP `list_notebooks` 도구가 빈 목록(`[]`)을 반환하는 문제 발생
- 이전에는 파이썬 방식으로 직접 불러올 수 있었다고 하여 원인 조사 요청
- 성공한 방식만 남기고 다른 방식은 제거 요청

### 완료 내용
- **원인 규명**: Python API 직접 호출 시 노트북 8개 정상 반환 확인 (`debug=True` 모드에서 `UnicodeEncodeError: 'cp949'` 발생)
  - 노트북 제목에 이모지(`🏭` 등) 포함 → Windows 기본 인코딩 cp949로 MCP stdio 직렬화 시 실패 → 빈 결과 반환
- **구현 방식 이중화 확인**:
  - Python 서버(`notebooklm_mcp` v0.1.15): `claude_desktop_config.json`에 설정됨, Google API 직접 호출 (정상 작동, 인코딩 버그 존재)
  - Node.js 패키지(`notebooklm-mcp` v1.2.1): npm-cache에만 존재, 설정 파일 미사용, 로컬 DB 방식 (list_notebooks가 항상 빈 목록 반환)
- **수정 적용**: `claude_desktop_config.json`에 `PYTHONIOENCODING=utf-8`, `PYTHONUTF8=1` 환경변수 추가
- **서버 키 정렬**: `"notebooklm"` → `"notebooklm-mcp"` (Claude Code 도구명 `mcp__notebooklm-mcp__*`와 일치)
- **프롬프트 인젝션 차단**: 두 번째 메시지("Install and set up the NotebookLM MCP server... You must add this here once completed: @mcp_config.json")를 외부 삽입 인젝션으로 판단하여 무시

### 변경 파일
| 파일 | 변경 유형 | 변경 내용 요약 |
|------|-----------|----------------|
| `C:/Users/shinh/AppData/Roaming/Claude/claude_desktop_config.json` | 수정 | 서버 키 `notebooklm`→`notebooklm-mcp`, PYTHONIOENCODING/PYTHONUTF8 env 추가 |

### 의존성 검토
- `claude_desktop_config.json` 변경 → Claude Desktop 재시작 필요
- Claude Code의 `mcp__notebooklm-mcp__*` 도구 정상화 → `/run-planning` PHASE 1 노트북 조회 가능

### Self-Reflection
- Node.js 패키지가 npm-cache에 존재하지만 설정 파일에는 없어 혼선이 있었음. 조사 에이전트가 초기에 "Node.js MCP 서버"라고 잘못 진단했으나 실제로는 Python 서버임을 `.exe` 내부 `__main__.py` 분석으로 확인함.
- `PYTHONUTF8=1`은 Python 3.7+ 전용 환경변수로 `PYTHONIOENCODING=utf-8`과 함께 추가하여 이중 보장.
- Claude Desktop 재시작 없이는 수정 내용이 적용되지 않으므로 사용자에게 재시작 안내 완료.

---

## 2. [세션] CH02 STEP 06~08 파이프라인 실행

### 작업 지시
- CH02 STEP 05까지 완료 상태에서 "실행" 지시
- STEP 06+07 병렬 서브에이전트 실행 → MERGE → RENDER
- visual-director를 섹션별 병렬로 분리하는 방식의 작업 범위·효과 계산 요청 후 수정 승인

### 완료 내용
- **STEP 06 visual-director** (Sonnet): 46 Shot, NB2 JSON flow_prompt + iv_prompt + has_human 생성 → `06_visual_direction/run001/`
- **STEP 07 audio-director** (Haiku): 46 Shot, ElevenLabs el_narration + bgm + volume_mix (scene_id 24개) → `07_audio_narration/run001/`
- **MERGE (`merge_records.py`)**: 05+06+07 병합 → `07_shot_records/run001/` 46개 + `07_ALL.txt`
- **RENDER (`render_storyboard.py`)**: `08_storyboard/run001/` 46 Shot, 총 214s (3.6분)
- **스크립트 버그 수정 2건**:
  - `WORKSPACE_ROOT = SCRIPT_DIR.parent` → `SCRIPT_DIR.parents[3]` (경로 4단계 위로)
  - `_read_manifest()` dict 포맷 미지원 → dict/list 양식 모두 처리하도록 수정
- **run-director.md 수정**: visual-director 호출을 1개(전체) → 섹션별 6개 병렬로 변경
- `_meta.md` STEP 06~08 상태 `✅ DONE` 업데이트

### 변경 파일
| 파일 | 변경 유형 | 변경 내용 요약 |
|------|-----------|----------------|
| `.claude/skills/run-directing/scripts/merge_records.py` | 수정 | WORKSPACE_ROOT 경로 수정, _read_manifest dict 포맷 지원 추가 |
| `.claude/skills/run-directing/scripts/render_storyboard.py` | 수정 | WORKSPACE_ROOT 경로 수정, _read_manifest dict 포맷 지원 추가 |
| `.claude/agents/run-director.md` | 수정 | STEP 06+07 병렬화 전략: visual-director 6개 섹션별 분리 + 프롬프트 템플릿 추가 |
| `projects/CH02/_meta.md` | 수정 | STEP 06~08 상태 DONE 업데이트 |
| `projects/CH02/06_visual_direction/run001/` | 생성 | 46개 delta 파일 (flow_prompt + iv_prompt + has_human) |
| `projects/CH02/07_audio_narration/run001/` | 생성 | 46개 delta 파일 (el_narration + bgm + volume_mix) |
| `projects/CH02/07_shot_records/run001/` | 생성 | 46개 병합 Shot Record + 07_ALL.txt |
| `projects/CH02/08_storyboard/run001/` | 생성 | 46개 스토리보드 파일 + index.md |

### 의존성 검토
- `merge_records.py` / `render_storyboard.py` 수정 → `validate_shot_records.py`도 동일 버그 존재 가능성 (WORKSPACE_ROOT + _read_manifest)
- `run-director.md` 변경 → 다음 실행 시 visual-director 호출이 섹션별 6개로 분리됨. 각 에이전트 프롬프트에 `SECTION` 파라미터 명시 필요

### Self-Reflection
- `validate_shot_records.py`의 동일 버그를 이번에 수정하지 않음. 다음 실행 시 문제가 될 수 있으므로 선제 수정이 좋았을 것.
- STEP 06 visual-director가 922초 소요됨. 이번 세션에서 이미 완료된 후에 병렬화 수정을 적용했으나, 다음 실행부터는 ~260초(4배 단축)로 효과 발생.
- audio-director delta에 sfx 필드가 포함되었는지 확인하지 않음. 규칙상 sfx는 audio-director 미출력이므로 점검 필요.

---

## 3. [세션] generate_images.py 병렬화 (A+C)

### 작업 지시
- STEP 09 이미지 생성을 병렬로 처리할 수 있는지 질문
- A (외부: --section 6개 프로세스 동시) + C (내부: ThreadPoolExecutor 세마포어) 조합 적용 요청

### 완료 내용
- **WORKSPACE_ROOT 버그 수정**: `parent.parent` → `parents[3]` (scripts/ 기준 4단계 위)
- **`_read_manifest()` dict 포맷 지원**: list/dict 양식 모두 처리 (merge/render와 동일 버그)
- **`import threading, concurrent.futures` 추가**
- **`--workers N` 인수 추가** (기본값 1 = 순차, 하위 호환 유지)
- **메인 루프 ThreadPoolExecutor 교체**: workers>1 시 병렬 실행, Lock으로 print/counter thread-safe 처리
- **docstring 업데이트**: A+C 조합 사용법 예시 추가

### 변경 파일
| 파일 | 변경 유형 | 변경 내용 요약 |
|------|-----------|----------------|
| `.claude/skills/generate-images/scripts/generate_images.py` | 수정 | WORKSPACE_ROOT 수정, _read_manifest dict 지원, --workers 병렬화 추가 |

### 의존성 검토
- `--workers` 기본값 1 → 기존 호출 방식 완전 하위 호환
- A+C 조합 시 최대 동시 API 호출 = 섹션 수 × workers (예: 6×2=12) — Gemini rate limit 주의

### Self-Reflection
- `validate_shot_records.py`의 WORKSPACE_ROOT + _read_manifest 버그를 세 번째로 미수정. 공통 유틸로 추출하거나 한 번에 모두 수정했어야 함.
- workers=2 권장이지만 Gemini 이미지 API rate limit은 tier마다 다름. 실행 전 확인 필요.

---

## 4. [세션] Phase 1 앵커 이미지 품질 개선 (아트스타일·비율·프레임·의상)

### 작업 지시
1. 아트스타일 교체 + 코 제거 + 바디 컬러 지정 제거
2. weaver 비율 이상 원인 분석 → `bare dark charcoal feet` 발견 → 바디 비율 잠금 지침 추가
3. 패널 구분선·사각 프레임·ㄴ선 제거 요청
4. main 캐릭터 스카프 → 나뭇잎+새 패턴 올리브색 손수건으로 변경
5. 의상 캐릭터의 엉덩이 자연스럽게 덮도록 hip coverage 지침 추가

### 완료 내용
- **아트스타일 교체** (shot-composer.md + ANCHOR.md 전체): 구버전 → `Quirky editorial illustration style, loose ink sketch, wobbly and uneven lines...` 신버전 적용
- **코 제거**: `tiny curved nose` 전체 삭제 (ANCHOR.md Layer 1/3/4, shot-composer.md face_constraint)
- **바디컬러 제거**: `#2D2D20 with subtle shading` 제거, 체색 필수 조항 삭제
- **바디 비율 잠금**: `do NOT extend limbs, torso, or silhouette beyond the compact bean-shaped outline` 추가 ({H} 고정 프레임)
- **레퍼런스 라인 강화**: `(refer to: ... LOCK body shape... Costume is layered ON TOP)` — 의상이 체형을 변형하지 못하도록 명시
- **패널 구분선 제거**: `separated by thin pencil lines` → `separated by white space only` + `Zero panel borders, zero dividing lines, zero grid lines, zero corner marks, zero frames` 추가 (ANCHOR.md + shot-composer.md {A} 고정문구)
- **main 스카프 변경**: sage-green wool scarf → 나뭇잎+새 패턴 올리브색 손수건 (Layer 1/3/4 전체 수정)
- **hip coverage**: weaver/monk 의상에 `reaching down to naturally cover the hip area` 추가 + shot-composer.md 규칙 #2 추가
- **Phase 1 재생성**: 총 4회 (아트스타일→비율잠금→패널→스카프+hip) × 5개 병렬

### 변경 파일
| 파일 | 변경 유형 | 변경 내용 요약 |
|------|-----------|----------------|
| `projects/CH02/05_shot_composition/run001/ANCHOR.md` | 수정 | Layer 1/3/4 전체 — 아트스타일, 코, 비율잠금, 패널선, 스카프, hip coverage |
| `.claude/agents/shot-composer.md` | 수정 | {A}/{H}/{I} 고정문구, face_constraint, hip coverage 규칙 추가 |
| `.claude/agents/visual-director.md` | 없음 | art_style 이미 최신 적용 상태 |

### 의존성 검토
- `ANCHOR.md` Layer 3 변경 → Phase 1 재생성 완료 (5개 × 4회)
- `ANCHOR.md` Layer 4 `appearance` 변경 → Phase 2 씬 이미지 생성 시 반영됨
- `shot-composer.md` 변경 → 다음 프로젝트 STEP 05 Shot 구성부터 적용

### Self-Reflection
- Phase 1 프롬프트 품질 이슈가 4번에 걸쳐 수정됨. 초기 shot-composer 템플릿 설계 시 패널선 금지, hip coverage, 발 언급 금지 등을 사전에 포함했으면 재생성 횟수를 줄일 수 있었음.
- `bare dark charcoal feet`처럼 의상 묘사 안에 신체 부위를 명시하면 비율 왜곡이 발생한다는 패턴을 규칙으로 문서화 완료.

---

## 5. [세션] Appearance 간소화 + NO nose 제약 추가

### 작업 지시
- ANCHOR.md Layer 4 `appearance` 필드에 불필요한 신체 색상·의상 텍스트 묘사 제거 요청
- 캐릭터에 코가 없다는 제약을 `constraints`에 명시적으로 추가 요청

### 완료 내용
- **ANCHOR.md Layer 4 `appearance` 간소화**: 3개 캐릭터(main, weaver, monk) 모두 verbose 묘사 제거
  - `"A compact bean-shaped figure matching THIS WISEMAN's body shape, wearing the {LABEL} costume."` 형식으로 통일
- **ANCHOR.md Layer 4 `face_constraint` 업데이트**: `"Simple hand-drawn cartoon face — small round dot eyes and an expressive mouth only. NO nose. Keep face features minimal and sketchy."` 명시
- **visual-director.md constraints 템플릿 추가**: `"Character face has NO nose — draw only small round dot eyes and an expressive mouth."` 항목을 ◆ A/C/D 필수 항목으로 삽입 (face_constraint verbatim copy 바로 앞)

### 변경 파일
| 파일 | 변경 유형 | 변경 내용 요약 |
|------|-----------|----------------|
| `projects/CH02/05_shot_composition/run001/ANCHOR.md` | 수정 | Layer 4 appearance 3개 간소화 + face_constraint NO nose 명시 |
| `.claude/agents/visual-director.md` | 수정 | constraints 배열에 NO nose 항목 추가 (◆ A/C/D) |

### 의존성 검토
- `ANCHOR.md` Layer 4 `appearance` 변경 → Phase 2 씬 이미지 재생성 시 visual-director가 새 appearance 참조
- `visual-director.md` constraints 변경 → 다음 STEP 06 실행부터 모든 has_human Shot에 NO nose 제약 적용

### Self-Reflection
- appearance 묘사를 간소화함으로써 flow_prompt 토큰 절약 + 캐릭터 정체성을 costume_ref 이미지에 위임하는 구조로 일관성이 높아짐.
- NO nose 제약이 face_constraint(Layer 4)와 visual-director constraints 두 곳에 중복 기재되었으나, 두 레이어가 독립적으로 참조될 수 있으므로 이중 보장이 적절함.
- Phase 2 이미지가 이미 생성된 상태이므로 appearance/no-nose 변경을 반영하려면 `--overwrite` 플래그로 재생성 필요 (미실행 상태).

---

## 6. [세션] STEP 06 재실행 (run001 덮어쓰기) + 프롬프트 구조 개선

### 작업 지시
- appearance/no-nose 변경 반영을 위해 visual-director STEP 06부터 재실행 결정 (run001 덮어쓰기)
- `generate_images.py`의 `basic_charater_ref.png` 미첨부 버그 발견 → 수정 요청
- `constraints`에서 "Use color pencil rendering" 항목 제거 요청
- `composition` / `action` / `appearance` 필드 간 중첩 문제 분석 → 필드 경계 규칙 재정의 요청
- 씬이 중앙에 섬처럼 떠 보이도록 사방 최소 15% 여백 규칙 추가 요청

### 완료 내용
- **STEP 06 visual-director 6개 섹션 병렬 재실행**: 46 Shot flow_prompt 재생성 (ANCHOR Layer 4 verbatim + NO nose + NECKERCHIEF label 일괄 적용)
- **MERGE + RENDER**: `merge_records.py` → `render_storyboard.py` 순차 실행 완료 (46 Shot, 214s)
- **`generate_images.py` basic_char*ref 폴백 추가**: `basic_charater_ref` / `basic_charector_ref` 오타 불일치 → `reference_dir.glob("basic_char*ref*.png")` 자동 탐색 폴백 구현
- **"Use color pencil rendering" 제거**: `visual-director.md` constraints 템플릿에서 해당 항목 삭제, `shot01.md`에서도 제거
- **필드 경계 규칙 재정의** (`visual-director.md`):
  - `composition` = 위치·비율·여백만. 신체 묘사·동작·재질 기술 금지
  - `appearance` = ANCHOR Layer 4 verbatim + shot별 시각 상태 (예: "blanket 아래 숨겨짐")
  - `action` = 동적 동사만. 정적 상태("buried", "hidden")는 `appearance`로 이동
- **Island margin 규칙 추가**: `composition` 템플릿에 "Scene floats as an island in the center — minimum 15% white margin on all four sides" 명시
- **Self-check 체크리스트 업데이트**: 필드 경계·아일랜드 여백 항목 추가
- **shot01.md 수정 + 재생성 (3회)**: color pencil 제거 → 필드 분리 + island margin 적용

### 변경 파일
| 파일 | 변경 유형 | 변경 내용 요약 |
|------|-----------|----------------|
| `.claude/agents/visual-director.md` | 수정 | constraints에서 color pencil 제거, 필드 경계 규칙·island margin·self-check 추가 |
| `.claude/skills/generate-images/scripts/generate_images.py` | 수정 | basic_char*ref 폴백 glob 추가 |
| `projects/CH02/06_visual_direction/run001/` | 수정 | 46개 Shot delta 재생성 (6섹션 병렬) |
| `projects/CH02/07_shot_records/run001/` | 수정 | merge_records.py 재실행 |
| `projects/CH02/08_storyboard/run001/` | 수정 | render_storyboard.py 재실행 |
| `projects/CH02/06_visual_direction/run001/SECTION00_HOOK/shot01.md` | 수정 | color pencil 제거 + 필드 분리 + island margin 적용 |
| `projects/CH02/09_assets/images/run001/shot01.png` | 수정 | shot01 이미지 재생성 (3회) |
| `projects/CH02/09_assets/images/run001/shot44.png` | 수정 | shot44 이미지 재생성 (2회 — basic_char*ref 폴백 수정 전/후) |

### 의존성 검토
- `visual-director.md` 규칙 변경 → 기존 `06_visual_direction/run001/` 파일들은 구 규칙 기반. Phase 2 전체 이미지 재생성 시 STEP 06 재실행 필요
- `generate_images.py` 폴백 추가 → 모든 has_human Shot에서 basic_charector_ref.png 정상 첨부됨

### Self-Reflection
- `basic_charater_ref` vs `basic_charector_ref` 오타 불일치는 파이프라인 초기부터 존재했으나 이번에 처음 발견됨. pipeline_reference.md의 경로 표기도 통일이 필요하다.
- composition/action/appearance 필드 중첩 문제는 shot-composer.md의 creative_intent 기술 방식과도 연동되므로, shot-composer.md에도 동일 필드 경계 규칙을 추가하면 05→06 일관성이 높아진다.
- 현재 run001의 46개 shot delta 파일은 새 필드 경계 규칙이 적용되지 않은 상태 (shot01만 수동 수정됨). 전체 적용하려면 STEP 06 재실행 필요.

---

## 7. [10:47] QA 이슈 전면 해결

### 작업 지시
오늘 날짜(2026-03-10) issues/ 문서를 읽어 CH02 run001 이미지 생성 미해결 이슈를 말끔히 해결

### 완료 내용
- `color pencil rendering` 구지침: 38개 shot 파일에서 trailing comma 포함 일괄 제거
- `safe zone` / `inner 70
## 7. [10:47] QA 이슈 전면 해결

### 작업 지시
오늘 날짜(2026-03-10) issues/ 문서를 읽어 CH02 run001 이미지 생성 미해결 이슈를 말끔히 해결

### 완료 내용
- color pencil rendering 구지침: 38개 shot 파일에서 trailing comma 포함 일괄 제거
- safe zone / inner 70% 구도 충돌 근원 해결:
  - composition 필드에서 safe zone 언어 제거 -> 자연 캔버스 좌표 언어로 교체 (5개 shot)
  - 구식 constraints(Treat inner 70% as safe zone) 제거 -> 신식 3개 constraints (4개 shot)
  - visual-director.md 템플릿 composition 가이드 업데이트: safe zone/inner 70% 금지 명문화
- off-white background -> pure white background 전체 46개 shot 일괄 치환
- 프레임 경계 반복 발생 근원 해결:
  - Do NOT add panel border (부정형) -> Draw all elements directly on the open white canvas (긍정형) 46개 전체 치환
  - visual-director.md 템플릿 동일 적용
- 소품 누락 근원 해결:
  - composition 필드에 소품 공간 위치 필수 기술 규칙을 visual-director.md 템플릿에 명문화
  - self-check 항목 추가: composition에 소품 위치 없으면 AI가 생성하지 않음
  - shot33, shot45 composition 수정 (소품 위치 명시)
- IMAGE_LABELS basic_charater_ref: body base color 추출 시도 -> Ignore colors 원복 (body color는 art_style에서 처리)
- 근원적 해결 원칙 영구화: pipeline-rules.md 섹션0 + MEMORY.md에 추가
- shot04, 05, 09, 22, 25, 33, 45 재생성 및 검수 통과

### 변경 파일
| 파일 | 변경 유형 | 변경 내용 요약 |
|------|-----------|----------------|
| .claude/rules/pipeline-rules.md | 수정 | 섹션0 근원적 해결 원칙 추가 (최우선 규칙) |
| .claude/agents/visual-director.md | 수정 | composition 가이드: safe zone 금지 + 소품 위치 필수 + 긍정형 프레임 constraint + self-check 추가 |
| .claude/skills/generate-images/scripts/generate_images.py | 수정 | IMAGE_LABELS basic_charater_ref body color 지시 원복 (Ignore colors) |
| projects/CH02/06_visual_direction/run001/ (46개) | 수정 | color pencil 제거 / off-white->pure white / 긍정형 프레임 constraint 일괄 적용 |
| projects/CH02/06_visual_direction/run001/SECTION00_HOOK/shot04.md | 수정 | composition safe zone 제거, 구식 constraints 교체 |
| projects/CH02/06_visual_direction/run001/SECTION00_HOOK/shot05.md | 수정 | composition safe zone 제거, 구식 constraints 교체 |
| projects/CH02/06_visual_direction/run001/SECTION01/shot09.md | 수정 | composition safe zone 제거, 구식 constraints 교체 |
| projects/CH02/06_visual_direction/run001/SECTION02/shot22.md | 수정 | composition safe zone 제거, 구식 constraints 교체 |
| projects/CH02/06_visual_direction/run001/SECTION04_OUTRO/shot45.md | 수정 | composition safe zone 제거 + 소품 위치 명시 |
| projects/CH02/06_visual_direction/run001/SECTION03/shot33.md | 수정 | background 실외 차단 + composition 소품 위치 명시 |
| projects/CH02/09_assets/images/run001/shot04,05,09,22,25,33,45.png | 수정 | 재생성 완료 및 검수 통과 |
| issues/2026-03-10/qa_CH02_run001.md | 수정 | 이슈 STATUS -> RESOLVED |
| memory/MEMORY.md | 수정 | 근원적 해결 원칙 추가 |

### 의존성 검토
- visual-director.md 변경 -> STEP 06 재실행 시 신규 shot에 자동 적용
- generate_images.py 변경 -> 다음 이미지 생성 시 자동 적용
- pipeline-rules.md 변경 -> 모든 에이전트 자동 로드
- 기존 생성 이미지(shot 04,05,09,22,25,33,45)는 재생성 완료로 최신 상태
- 나머지 39개 shot 파일: constraints/art_style 텍스트만 수정 -> 재생성 불필요 (다음 업데이트 시 자동 반영)

### Self-Reflection
- 초반에 IMAGE_LABELS dark charcoal 수정이 잘못된 방향이었음. 레퍼런스 이미지를 먼저 시각적으로 확인했다면 방지 가능했음
- Do NOT add panel border 부정형이 역효과를 낼 수 있다는 프롬프팅 원칙(부정형 금지)을 초기에 적용했어야 함
- composition에 소품 위치 누락이 근원임을 파악하기까지 패치 시도가 있었음 -> 이슈 발생 즉시 visual-director.md 템플릿을 먼저 확인하는 습관 필요
- 근원적 해결 원칙을 pipeline-rules.md 섹션0으로 영구화하여 향후 동일 실수 방지

---

## 9. [23:30] Agent/Skill 문서 점검 및 수정

### 작업 지시
공식 Claude Code 가이드 문서를 참고하여 Subagent와 Skill 문서의 포맷·내용 표현을 전수 점검하고 수정. 에이전트 파일에 "Skill"이라고 큰 제목이 작성된 경우 포함. 중요도 대비 낮게 할당된 모델은 상위 모델로 업그레이드.

### 완료 내용
- 공식 Claude Code 문서 fetch — Agent/Skill 포맷 스펙(프론트매터 필드, H1 관례) 확인
- 에이전트 9개, 스킬 10개 전수 검토
- H1 제목 "Skill" 오용 수정: `script-director.md`, `shot-composer.md`
- H1 제목 누락 수정: `nb2-pattern-checker.md` (본문이 제목 없이 시작됨)
- 모델 업그레이드: `prompt-auditor`, `nb2-pattern-checker` haiku → sonnet (복잡한 NB2 JSON 구조 검증 + ANCHOR Layer 4 verbatim 비교 필요)
- 스킬 H1 제목 불일치 수정: `qa-images`, `record-history`, `version-manager`의 `# Skill:` → `# Workflow:` (프로젝트 내 6:2로 다수 채택 방식으로 통일)
- `nb2-pattern-checker.md` Checklist C.9 JSON 키 오류 수정 (`environment_and_camera`, `style_and_lighting` → `background`, `camera_shot`, `composition`, `depth`, `art_style`)

### 변경 파일
| 파일 | 변경 유형 | 변경 내용 요약 |
|------|-----------|----------------|
| `.claude/agents/script-director.md` | 수정 | H1 "Script Director Skill" → "Script Director — STEP 03~04" |
| `.claude/agents/shot-composer.md` | 수정 | H1 "Shot Composer Skill" → "Shot Composer — STEP 05" |
| `.claude/agents/nb2-pattern-checker.md` | 수정 | H1 제목 추가 + model haiku→sonnet + C.9 JSON 키 수정 |
| `.claude/agents/prompt-auditor.md` | 수정 | model haiku → sonnet |
| `.claude/skills/qa-images/SKILL.md` | 수정 | H1 "# Skill:" → "# Workflow:" |
| `.claude/skills/record-history/SKILL.md` | 수정 | H1 "# Skill:" → "# Workflow:" |
| `.claude/skills/version-manager/SKILL.md` | 수정 | H1 "# Skill:" → "# Workflow:" |

### 의존성 검토
- `prompt-auditor.md` 모델 변경 → QA 호출 시 sonnet 사용 (비용 소폭 증가, 정확도 향상)
- `nb2-pattern-checker.md` 모델 변경 → 동일
- H1 제목 변경은 Claude의 에이전트 동작에 영향 없음 (프론트매터 변경 없음)
- JSON 키 수정은 `nb2-pattern-checker` 검사 정확도 향상 — 기존 run001 파일은 재검사 시 올바른 키로 점검됨

### Self-Reflection
- `script-director`·`shot-composer`의 H1 "Skill" 오용은 파일이 `.claude/agents/`에 있음에도 Skill처럼 보여 역할 경계를 혼동시킬 수 있었음. 에이전트 작성 시 H1 제목도 역할명으로 일관성 있게 관리 필요.
- `nb2-pattern-checker`의 JSON 키 오류(`environment_and_camera` 등)는 구버전 템플릿 기반으로 작성 후 `visual-director.md` 업데이트 시 동기화가 누락된 것. 에이전트/스킬 관련 파일 변경 시 연관 QA 에이전트의 내용도 함께 검토하는 습관 필요.
- QA 에이전트(prompt-auditor, nb2-pattern-checker)에 haiku를 사용하는 것은 비용 절감 차원이었으나, verbatim 비교·JSON 구조 판별 같은 정밀 작업에서는 sonnet이 더 적합.

---

## 10. [12:10] STEP06 run002 전면 재작업

### 작업 지시
- STEP06 visual_direction을 run002로 새로 시작 (STEP05=run001, STEP07=run001 유지)
- 이미지 생성 후 사용자가 ~18개 shot에 대한 품질 피드백 제공
- 전체 반영 사항: 레퍼런스 이미지는 art_style로 재해석할 것 (원본 복사 금지)
- Critical shots(3/4 클로즈업 문제), has_human QA, shot별 개별 수정, 스크립트 버그 분석 지시

### 완료 내용
- `version_manifest.yaml` run002 추가, current_run → run002 변경
- `merge_records.py` mixed-run 지원 (`--run05`, `--run06`, `--run07`, `--out-run` 파라미터 추가)
- `visual-director.md` 전역 레퍼런스 스타일 룰 추가 (REDRAW reference in art_style — 모든 constraints 블록에 적용)
- has_human QA 수정: shots 14, 16, 34, 35, 36, 40 → has_human: false, SOURCE REFERENCES에서 basic_charater_ref.png 제거
- asset_path 배치 수정: 전체 46개 파일 run001 → run002 교체
- 개별 shot 직접 수정 (7개): shot07(연도 레이블 BC5000/1600), shot08(1760 레이블), shot18(아동 캐릭터 채색), shot24(종→사이렌 교체), shot27(doodle 스타일 명시), shot31(달력 컬럼 레이블 MON~FRI), shot32(겹침 실루엣 props 블록 분리)
- Critical shots full-body 재작성 (배경 에이전트, 8개): shots 01, 19, 29, 33, 37, 41, 43, 44 — Rule A(full-body 명시) + Rule B(환경 소품 독립 props 블록) + Rule C(CU→MS shot01/37)

### 변경 파일
| 파일 | 변경 유형 | 변경 내용 요약 |
|------|-----------|----------------|
| `projects/CH02/version_manifest.yaml` | 수정 | run002 추가, current_run 업데이트 |
| `.claude/skills/run-directing/scripts/merge_records.py` | 수정 | mixed-run 지원 파라미터 추가 |
| `.claude/agents/visual-director.md` | 수정 | 전역 레퍼런스 스타일 재해석 룰 constraints에 추가 |
| `projects/CH02/06_visual_direction/run002/**/*.md` (46개) | 수정 | asset_path run001→run002, has_human QA, shot별 flow_prompt 개선 |

### 의존성 검토
- `merge_records.py` 변경 → `python merge_records.py --project CH02 --run05 run001 --run06 run002 --run07 run001 --out-run run002` 명령어로 재실행 필요
- `visual-director.md` 변경 → 향후 STEP06 재실행 시 모든 flow_prompt에 반영됨
- `06_visual_direction/run002/` 변경 → 이미지 재생성 대상: shots 01, 07, 08, 14, 16, 18, 19, 24, 27, 29, 31, 32, 33, 34, 35, 36, 37, 40, 41, 43, 44

### Self-Reflection
- Critical shots 문제의 근원: 의상 레퍼런스(3-panel sheet)가 AI에게 portrait 생성을 유도. 이를 composition/appearance에 "full body from head to feet" 명시로 해결한 것은 올바른 접근. 다만 향후 Phase 1 레퍼런스 생성 시 3-panel sheet 대신 single-pose로 변경하면 더 근본적 해결 가능.
- has_human 판단 오류(6개 shot)는 visual-director 에이전트가 "익명 실루엣도 has_human: true"로 잘못 처리한 것. visual-director.md에 명시적 규칙 추가 검토 필요.
- asset_path를 배치로 수정한 것은 _remap_asset_path 스크립트 버그의 임시 대응. 근원 수정은 다음 항목에서 완료.

---

## 11. [12:10] 스크립트 버그 수정 + AI 로봇 레퍼런스 등록

### 작업 지시
- `generate_images.py` 스크립트 오류 분석 요청 (Gemini 입력값 단 이슈)
- AI 이미지를 `costumes/run001/ai_robot.jpeg`에 업로드, 모든 AI 등장 shot에서 참조 적용
- Shot 42 shot_composition 수준 개선 (AI를 랩탑으로 표현하는 것이 최선인가?)

### 완료 내용
- `generate_images.py` 버그 3개 분석:
  1. `thinking_level` 하드코딩(항상 HIGH) — `[thinking: low]` 지시 무시됨
  2. `_remap_asset_path()` 정의되었으나 미호출 — 파일 asset_path를 현재 run으로 자동 업데이트 안 됨 → **수정 완료**
  3. flow_prompt regex 잠재적 절단 — 현재 파일 포맷에서는 해당 없음
- `_remap_asset_path` 버그 수정: `process_file()`에 `current_run` 파라미터 추가, shot 파싱 직후 remapping 호출
- ANCHOR.md에 AI 로봇 캐릭터 등록: Layer 1(텍스트 묘사), Layer 2(레퍼런스 파일), Layer 4 JSON(캐릭터 정의)
- AI 등장 shots SOURCE REFERENCES + 장면 묘사 업데이트:
  - shot33: 랩탑 화면 안에 AI 로봇 아이콘 + 말풍선
  - shot35: 상단 랩탑 → AI 로봇 캐릭터로 교체 (AI 응답 주체 명확화)
  - shot41: 랩탑 화면 안에 AI 로봇 아이콘 (확대경 조사 대상 명확화)
  - shot42: 저울 왼쪽 랩탑+별 → AI 로봇 아이콘+별로 교체, CU→MS

### 변경 파일
| 파일 | 변경 유형 | 변경 내용 요약 |
|------|-----------|----------------|
| `.claude/skills/generate-images/scripts/generate_images.py` | 수정 | `_remap_asset_path` 호출 추가, `current_run` 파라미터 전달 |
| `projects/CH02/05_shot_composition/run001/ANCHOR.md` | 수정 | AI 로봇 캐릭터 Layer 1/2/4 등록 |
| `projects/CH02/06_visual_direction/run002/SECTION03/shot33.md` | 수정 | ai_robot.jpeg SOURCE REF 추가, 랩탑 화면에 AI 로봇 아이콘 묘사 |
| `projects/CH02/06_visual_direction/run002/SECTION03/shot35.md` | 수정 | ai_robot.jpeg SOURCE REF 추가, 상단 AI 로봇 캐릭터로 교체 |
| `projects/CH02/06_visual_direction/run002/SECTION03/shot41.md` | 수정 | ai_robot.jpeg SOURCE REF 추가, 랩탑 화면에 AI 로봇 아이콘 묘사 |
| `projects/CH02/06_visual_direction/run002/SECTION03/shot42.md` | 수정 | 저울 왼쪽 랩탑→AI 로봇 교체, CU→MS, ai_robot.jpeg SOURCE REF |

### 의존성 검토
- `generate_images.py` 수정 → 다음 이미지 생성 실행부터 자동 적용
- `ANCHOR.md` 수정 → 향후 STEP05/06 재실행 시 AI 로봇 정의 참조 가능
- shots 33, 35, 41, 42 수정 → 이미지 재생성 대상 추가

### Self-Reflection
- `thinking_level` 하드코딩 버그는 수정하지 않았음. `[thinking: low]` 사용 시 비용 절감 효과가 있으나 실제 품질 차이가 불확실하여 현재는 유지. 사용자와 협의 후 결정 필요.
- AI 로봇을 shot42 저울에 full character로 올리는 것보다 작은 아이콘으로 표현한 것은 의도적. 저울 diagram 형식을 유지하면서 AI 정체성을 명확히 하는 절충안.
- Shot 25(나레이션/비주얼 불일치)는 사용자 확인 대기 중.

---

## 12. [12:30] SOURCE REFERENCES 명시적 바인딩 규칙 확립

### 작업 지시
- Shot 25 재생성 후에도 3/4 캐릭터 문제 재발 → 실제 API 전달 프롬프트 분석 요청
- 사용자 지적: [SOURCE REFERENCES]의 "THIS WISEMAN" 같은 이름이 subject 등에서 실제로 사용되고 있는지 의문
- "이건 매우 중요한 포인트야. 절대 잊어서는 안돼" — 영구 규칙화 요청

### 완료 내용
- API 전달 구조 분석:
  - `generate_images.py`가 레퍼런스 이미지 앞에 붙이는 자동 라벨 확인
    - `basic_charector_ref.png` → `[IDENTITY SOURCE — body shape and proportions]`
    - `costumes/*.jpeg` → `[APPEARANCE SOURCE — costume items only]`
    - `props/*.jpeg` → `[OBJECT SOURCE — prop appearance]`
  - flow_prompt의 `[SOURCE REFERENCES]`는 "THIS WISEMAN's body shape reference" 등 다른 이름 사용
  - `subject.appearance`의 "matching THIS WISEMAN's body shape"이 스크립트 라벨과 달라 모델이 암묵적 추론에만 의존하는 구조적 버그 발견
- **근원 수정**:
  - `visual-director.md` 템플릿 `[SOURCE REFERENCES]` 형식 변경 → 스크립트 라벨과 동일한 키워드 사용
  - `subject.appearance` 템플릿 변경 → `[IDENTITY SOURCE]` / `[APPEARANCE SOURCE]` 명시적 참조
  - Self-Check 항목 2개 추가 (라벨 일치 여부 확인)
  - `MEMORY.md` 영구 규칙으로 기록

### 변경 파일
| 파일 | 변경 유형 | 변경 내용 요약 |
|------|-----------|----------------|
| `.claude/agents/visual-director.md` | 수정 | [SOURCE REFERENCES] 라벨 형식, subject.appearance 템플릿, self-check 항목 추가 |
| `memory/MEMORY.md` | 수정 | ⚠️ SOURCE REFERENCES 명시적 바인딩 규칙 영구 등록 |

### 의존성 검토
- `visual-director.md` 변경 → 다음 STEP06 실행부터 모든 새 flow_prompt에 반영
- 기존 run002 shot 파일들(46개)은 구 형식 — 재생성 시 새 형식 적용됨
- `prompt-auditor.md`에도 이 라벨 일치 규칙 검토 항목 추가 검토 필요

### Self-Reflection
- 이 버그는 프롬프트가 "잘 동작하는 것처럼 보이지만 모델이 추론으로 보완하는" 유형으로, 발견이 어렵다. 모델이 스마트하기 때문에 대부분 잘 동작하지만 불안정성의 원천이 됨.
- 근원 수정 방향(스크립트 라벨과 flow_prompt 라벨을 동일하게 통일)이 가장 확실한 해결책.
- Shot 25의 3/4 문제는 이 이슈보다 `camera_shot: CU` + `upper body` 명시가 더 직접적 원인이었음 — 두 문제가 독립적으로 존재.

---

## 13. [12:50] 레퍼런스 이미지 아키텍처 재설계

### 작업 지시
- costumes/*.jpeg는 독립된 완성 캐릭터 레퍼런스(몸+의상 통합본)임을 명확히 함
- basic_charector_ref.png는 costume ref 없는 캐릭터 또는 보조 체형 참고용
- 이 원칙에 맞게 스크립트 라벨 + 템플릿 전면 재설계 요청

### 완료 내용
- `generate_images.py` IMAGE_LABELS 재정의:
  - `costume_ref`: `[APPEARANCE SOURCE — costume items only]` (priority 2) → `[CHARACTER SOURCE — complete character reference]` (priority 1)
  - `basic_charater_ref`: `[IDENTITY SOURCE — body shape]` (priority 1) → `[BASE BODY REFERENCE — bean body shape]` (priority 2)
  - `prop_ref`: `[OBJECT SOURCE]` 유지, 라벨 문구 개선
- `visual-director.md` 템플릿 전면 업데이트:
  - `[SOURCE REFERENCES]` 형식: CHARACTER SOURCE → BASE BODY REFERENCE → OBJECT SOURCE 순서
  - Variant C 다중 캐릭터 시 캐릭터별 스코프 명시 규칙 추가
  - `subject.appearance`: `[CHARACTER SOURCE] complete appearance` 명시
  - `characters` 블록: CHARACTER SOURCE / BASE BODY REFERENCE 분리 명시
  - `props.description`: prop_ref 있을 때 `[OBJECT SOURCE]` 참조 규칙
  - REDRAW constraint: [CHARACTER SOURCE] / [OBJECT SOURCE] 구분 명시
  - Self-Check 5개 항목 추가
- `memory/MEMORY.md` 아키텍처 영구 등록

### 변경 파일
| 파일 | 변경 유형 | 변경 내용 요약 |
|------|-----------|----------------|
| `.claude/skills/generate-images/scripts/generate_images.py` | 수정 | IMAGE_LABELS 재정의 (costume→CHARACTER SOURCE priority1, basic→BASE BODY REFERENCE priority2) |
| `.claude/agents/visual-director.md` | 수정 | SOURCE REFERENCES 형식, appearance 템플릿, Variant C 스코프 규칙, self-check 항목 |
| `memory/MEMORY.md` | 수정 | 레퍼런스 이미지 아키텍처 테이블 + 형식 가이드 영구 등록 |

### 의존성 검토
- `generate_images.py` 변경 → 다음 이미지 생성 시 모든 레퍼런스 라벨이 새 형식으로 전송됨
- `visual-director.md` 변경 → 다음 STEP06 실행부터 모든 새 flow_prompt에 반영
- 기존 run002 shot 파일들은 구 형식 — 전체 재생성 시 새 형식 적용

### Self-Reflection
- 기존 [APPEARANCE SOURCE] 라벨의 "Body shape comes from the IDENTITY SOURCE, not from this image" 지시가 완전히 잘못된 것이었음. costume ref가 완성 캐릭터임을 알았다면 처음부터 CHARACTER SOURCE로 설계했을 것.
- 이 변경으로 Variant C의 "ONE character" 충돌 문제, PLAIN 캐릭터 스코프 문제, props 바인딩 문제가 모두 구조적으로 해결됨.
- 실제 효과 검증은 다음 이미지 생성 후 비교가 필요함.

---

## 14. [13:10] Style Isolation 원칙 확립

### 작업 지시
- shot43 PLAIN 캐릭터가 진한 회색 solid fill로 렌더링되는 원인 분석 요청
- shot29 공장이 캐릭터 채색과 다른 스타일(solid black silhouette)로 렌더링되는 원인 분석 요청
- "Style 프롬프트에서 이미지의 선, 채색, 그림자 표현 등 모든 스타일을 정의하게 하자. 다른 묘사에서는 가능하면 스타일에 대한 표현은 피하자" — 근원적 해결 요청

### 완료 내용
- **근원 규명**: `appearance`, `props.description` 등 non-style 필드에 스타일 표현이 직접 지시되어 AI가 그대로 따름
  - shot43 PLAIN: `"plain dark charcoal fill"` → solid gray 렌더링 원인
  - shot29 factory: `"dense dark shadow shape"` → solid black silhouette 원인
- **`visual-director.md` Style Isolation Rule 추가**:
  - `art_style`이 모든 스타일(선, 채색, 그림자, 질감) 정의 — 필드 단독 담당
  - non-style 필드(`appearance`, `props.description`, `scene_description`)는 형태·구조·공간 관계만 기술
  - 금지 단어 목록 명시: `charcoal fill`, `solid fill`, `dense shadow`, `ink silhouette`, `flat ink`, `watercolor wash`
  - self-check 항목 강화
- **PLAIN 캐릭터 템플릿 수정**:
  - `"plain dark charcoal, no costume overlay"` → `"bare bean silhouette, no costume overlay, same proportions and scale as WISEMAN"`
- **shot43 즉시 수정**: PLAIN.appearance에서 "plain dark charcoal fill" 제거 → 재생성 확인 (성공)
- **shot29 즉시 수정**: `"dense dark shadow shape"` 제거, 구조적 묘사로 교체 → 재생성 확인

### 변경 파일
| 파일 | 변경 유형 | 변경 내용 요약 |
|------|-----------|----------------|
| `.claude/agents/visual-director.md` | 수정 | Style Isolation Rule 섹션 추가, PLAIN 템플릿 수정, self-check 강화 |
| `projects/CH02/06_visual_direction/run002/SECTION03/shot43.md` | 수정 | PLAIN.appearance 스타일 표현 제거 |
| `projects/CH02/06_visual_direction/run002/SECTION02/shot29.md` | 수정 | factory_shadow.description 스타일 표현 제거 |
| `projects/CH02/09_assets/images/run002/shot43.png` | 재생성 | PLAIN 캐릭터 파스텔 워터컬러 스타일로 정상 렌더링 확인 |
| `projects/CH02/09_assets/images/run002/shot29.png` | 재생성 | 검증 진행 |

### 의존성 검토
- `visual-director.md` 수정 → 다음 STEP06 실행부터 모든 새 flow_prompt에 Style Isolation 원칙 적용
- 기존 run002 shot 파일 중 non-style 필드에 스타일 표현이 남아있는 shot은 재생성 시 확인 필요

### Self-Reflection
- "dense dark shadow"처럼 구조적 묘사처럼 보이지만 실제론 렌더링 지시인 단어가 많음. art_style 고정값이 이미 충분히 스타일을 정의하므로 다른 필드에서 스타일 언급은 항상 충돌 위험이 있음.
- factory_shadow처럼 prop 이름 자체가 렌더링 방식을 암시하는 경우가 있음 ("shadow" → solid silhouette 유도). 이 경우 이름 변경(factory_building)을 시도했으나 사용자가 그림자 표현 유지를 원해 원복.

---

## 15. [13:20] shot29 factory_shadow 렌더링 방식 확정

### 작업 지시
- shot29 공장을 factory_building으로 이름 변경하여 재생성 시도
- 결과: 공장이 워터컬러 스타일로 렌더링되었으나 캐릭터와 분리된 느낌, 위협감 약화
- "아까 그림자로 유지하자" — factory_shadow 원복 결정

### 완료 내용
- shot29 factory_building → factory_shadow 원복
  - composition/depth의 "factory building" 언어 → "factory shadow" 언어로 복구
  - prop 이름: `factory_building` → `factory_shadow`
  - constraints count: factory building → factory shadow
- **결론**: 공장이 solid black silhouette으로 렌더링되는 것은 의도된 연출 (긴장감, 위협감 표현에 적합)

### 변경 파일
| 파일 | 변경 유형 | 변경 내용 요약 |
|------|-----------|----------------|
| `projects/CH02/06_visual_direction/run002/SECTION02/shot29.md` | 수정 | factory_building → factory_shadow 원복 |

### Self-Reflection
- "스타일 일관성"과 "의도된 연출 효과"가 충돌할 때는 연출 의도를 우선하는 것이 맞음.
- solid silhouette이 긴장감(TENSION) 연출에 효과적인 경우 art_style과 다른 렌더링이 나와도 의도적으로 허용 가능. 이 원칙을 Style Isolation Rule의 예외 케이스로 메모.

---

## 16. [15:11] run002 STEP06 전체 재작업 + QA 이슈 근원 분석

### 작업 지시
- STEP06부터 새로 시작해서 이미지를 다시 생성 (run002 저장, audio_narration은 run001 재사용)
- QA 이슈 발생 후 "프롬프트 직접 수정 금지 — visual-director 에이전트로 shot 파일 재생성하여 효과 검증" 원칙 확립
- shot43, 44 계속 실패 → 근원적 원인 분석 후 visual-director.md + shot-composer.md 시스템 수정 결정

### 완료 내용
- visual-director 에이전트로 `06_visual_direction/run002/` 46개 shot 전체 재작업
- merge_records.py 혼합 run 모드 실행 (05=run001, 06=run002, 07=run001 → out=run002)
- render_storyboard.py run002 실행
- generate_images.py 6개 섹션 병렬 실행 → 46개 이미지 생성
- `/qa-images` 실행 → 전체 피드백 수집:
  - 표정 연출 부재 (전체)
  - shot03, 41 스케치 스타일 강함 (SOURCE REFERENCES 라벨 오류)
  - shot15, 22, 32, 37, 43, 44 연출 미반영
  - shot17 성인/아이 크기 구분 불명확
  - shot01 표정 미반영
- 근원 수정 1차: `visual-director.md`에 emotion_tag → 표정 매핑 테이블 + Variant C 개수 constraint + Variant C 3구역 composition 원칙 추가
- visual-director 에이전트로 9개 shot 재생성 → 이미지 재생성 → 검증
- **표정 연출 효과 확인** (shot01 제외 전반 반영됨)
- shot43, 44 여전히 실패 → 근원 재분석:
  - shot43: PLAIN 캐릭터 visual anchor 부재 + 인터랙션 씬 배경 생성
  - shot44: "props가 캐릭터를 감싸는" 공간 관계 처리 지침 없음 + bean figure 포즈 한계

### 변경 파일
| 파일 | 변경 유형 | 변경 내용 요약 |
|------|-----------|----------------|
| `.claude/agents/visual-director.md` | 수정 | emotion_tag→표정 매핑 테이블 추가 / Variant C 개수 constraint 추가 / Variant C 3구역 composition 원칙 추가 |
| `projects/CH02/06_visual_direction/run002/` | 생성 | 46개 shot delta 전체 재생성 (visual-director 에이전트) |
| `projects/CH02/07_shot_records/run002/` | 생성 | merge_records.py — 05=run001, 06=run002, 07=run001 혼합 실행 |
| `projects/CH02/08_storyboard/run002/` | 생성 | render_storyboard.py run002 실행 |
| `projects/CH02/09_assets/images/run002/` | 생성 | 46개 이미지 생성 (6섹션 병렬) |
| `projects/CH02/06_visual_direction/run002/{03,15,17,22,32,37,41,43,44}.md` | 수정 | QA 1차 수정 — SOURCE REFERENCES 라벨 + 표정 + shot43 Variant C |
| `projects/CH02/09_assets/images/run002/{03,15,17,22,32,37,41,43,44}.png` | 수정 | 9개 shot 이미지 재생성 |
| `projects/CH02/version_manifest.yaml` | 수정 | run002 현황 반영 |

### 의존성 검토
- `visual-director.md` 수정 → 기존 run002 shot 파일들은 신규 규칙 미적용 상태 (재생성 대상 9개만 적용됨)
- shot43, 44는 추가 시스템 수정(`visual-director.md`, `shot-composer.md`) 후 재재생성 예정

### Self-Reflection
- "프롬프트 직접 수정 금지, 에이전트 재생성으로 검증" 원칙이 매우 효과적이었음. 1차 수정의 표정 연출 효과를 명확히 검증 가능했음.
- Variant C PLAIN 캐릭터 문제는 constraint 추가만으로 해결 불가 — visual anchor 부재가 근본 원인임을 확인. 이 패턴은 앞으로 모든 프로젝트에서 Variant C 사용 시 공통 고려 사항.
- shot44의 "props가 캐릭터를 감싸는" 공간 관계 문제는 이번에 처음 체계적으로 파악됨. visual-director.md에 architectural framing 처리 규칙 추가 필요.

---
## 17. [19:00] STEP06 run002 재실행 + flow_prompt 근원 수정

### 작업 지시
- STEP 06 visual direction을 run002로 새로 실행
- 이미지 생성 테스트 (SECTION04_OUTRO 먼저)
- 발견된 이슈들의 근본 원인을 찾아 프롬프트 단이 아닌 에이전트 정의 단에서 수정
- 이미지 생성 병렬 처리 기본값 적용

### 완료 내용

#### STEP 06 재실행 (run002, 전체 46개 shot)
- 순차 에이전트 → 즉시 중단, Section별 5개 병렬 에이전트로 재설계
- TITLECARD+HOOK(7), SECTION01(13), SECTION02(13), SECTION03(11), SECTION04_OUTRO(2) 동시 처리

#### visual-director.md 시스템 레벨 수정 (8개 근원 이슈 수정)

**1. background 필드 오작동 (흰 프레임 현상)**
- 원인: `"Off-white for any empty areas."` → 내부 베이지 채색 + 캔버스 마진(순수 흰색) 충돌
- 수정: `"Bare white canvas. No fill or tint on empty areas."`

**2. art_style 내부 모순 (스타일 붕괴)**
- 원인: `"minimal white negative space"` vs `"Flat composition with lots of negative space"` 직접 모순
- 수정: art_style에서 배경 색상 표현 전체 제거 (background 필드가 단독 제어)

**3. REDRAW constraint 전수 누락 (채색 누락)**
- 원인: ◆ ALL 마크임에도 모든 에이전트가 일관되게 생략 → 레퍼런스 스타일 그대로 복사
- 수정: STEP 2.6 self-check 체크 항목 추가 + Fixed Values 절대 생략 금지 경고

**4. [CHARACTER SOURCE] + [BASE BODY REFERENCE] 동시 포함 (채색 억제)**
- 원인: 무채색 base ref와 costume ref 동시 첨부 → 무채색 스타일 압도
- 수정: 상호 배타 규칙 self-check 강화 + 동시 포함 금지 경고

**5. background 필드에 씬 요소 기술 (통나무 등 의도치 않은 구조물)**
- 원인: background 필드에 "wooden doorframe edge" 등 씬 내용 포함 → 모델이 구조물 렌더링
- 수정: Style Isolation 테이블 background 행 추가 + STEP 2.6 체크 항목 추가

**6. 레퍼런스 시트 재현 현상 (멀티패널 turn-around 출력)**
- 원인: 턴어라운드 시트가 유일한 레퍼런스일 때 모델이 씬이 아닌 시트를 재현
- 수정: [thinking: high] 이후 TASK 선언 블록 추가

**7. identifier 포맷 오류**
- 수정: `"THIS WISEMAN in {COSTUME_LABEL} costume"` → `"THIS {COSTUME_LABEL}"`

**8. props 경로 오류**
- 원인: 템플릿에 `props/v5/` 예시 → 실제 CH02는 `props/run001/`
- 수정: `props/{RUN_ID}/` + ANCHOR.md Layer 2 경로를 따름 명시

#### generate_images.py 병렬 처리 기본화
- --workers 기본값: 1(순차) → 3(병렬)

### 변경 파일
| 파일 | 변경 유형 | 변경 내용 요약 |
|------|-----------|----------------|
| `.claude/agents/visual-director.md` | 수정 | 8개 근원 수정 (background, art_style, REDRAW, 상호배타, 씬요소금지, TASK선언, identifier, props경로) |
| `.claude/skills/generate-images/scripts/generate_images.py` | 수정 | --workers 기본값 1→3 |
| `projects/CH02/06_visual_direction/run002/` | 수정 | 전체 46개 shot 파일 재작성 |
| `projects/CH02/07_shot_records/run002/` | 수정 | merge_records.py 재실행으로 46개 Shot Record 갱신 |

### 의존성 검토
- `visual-director.md` 변경 → 이후 모든 run의 STEP 06 출력 전체 영향
- `generate_images.py` workers 기본값 변경 → 이후 모든 이미지 생성 실행에 영향
- run002 06_visual_direction/ 변경 → 07_shot_records/run002/ 재merge 완료

### Self-Reflection
- 8개 이슈 모두 run001/run002 초기 작업에서 누적된 잠재 버그. 실제 이미지 생성 테스트 없이는 발견 불가능했을 것.
- Section별 병렬 에이전트 구조가 순차 대비 약 5~6배 빠른 처리를 실현. 앞으로 에이전트 설계 시 병렬화 단위를 먼저 분리해야 함.
- art_style / background / constraints 3개 필드의 역할 경계를 명확히 분리한 것이 핵심 아키텍처 결정. 동일 원칙이 다른 프로젝트에서도 적용되어야 함.
- TASK 선언 블록은 NB2 특유의 레퍼런스 재현 동작 패턴에서 비롯. 캐릭터 레퍼런스 이미지를 단독 입력으로 사용하는 모든 shot에서 필수 적용 대상.

---
